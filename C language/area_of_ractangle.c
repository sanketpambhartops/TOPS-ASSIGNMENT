#include<stdio.h>
int main()
{
    float length,width,area;
    printf("enter the length \n",length);
    scanf("%f",&length);
    printf("enter the width \n",width);
    scanf("%f",&width);
    area=length*width;
    printf("the area of ractangle is %f",area);
    return 0;
}